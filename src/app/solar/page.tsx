import { SolarView } from './SolarView';

export const metadata = {
  title: 'Solar',
  description: 'Earth’s daylight, twilight, and your local path of the Sun.',
};

export default function SolarPage() {
  return <SolarView />;
}
